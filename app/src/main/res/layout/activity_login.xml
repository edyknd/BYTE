<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".LoginActivity">

    <ImageView
        android:id="@+id/imglogo"
        android:layout_width="170dp"
        android:layout_height="170dp"
        android:layout_marginTop="30dp"
        android:contentDescription="@string/logo_byte"
        android:src="@drawable/ic_logo"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="60dp"
        android:layout_marginStart="35dp"
        android:layout_marginEnd="35dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/imglogo">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/usuario"
            android:textColor="@color/grey_text"
            android:textSize="12sp" />

        <EditText
            android:id="@+id/txtUsuario"
            android:layout_width="match_parent"
            android:layout_height="@dimen/height_size_items_login"
            android:layout_marginTop="3dp"
            android:background="@drawable/shape_text_login"
            android:gravity="center_vertical"
            android:inputType="text"
            android:maxLines="1"
            android:paddingStart="@dimen/pading_text_login"
            android:paddingEnd="@dimen/pading_text_login"

            android:textColor="@color/colorPrimary"
            android:textSize="15sp"
            android:importantForAutofill="no" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/contrase_a"
            android:textColor="@color/grey_text"
            android:textSize="12sp" />

        <EditText
            android:id="@+id/txtContraseña"
            android:layout_width="match_parent"
            android:layout_height="@dimen/height_size_items_login"
            android:layout_marginTop="3dp"
            android:background="@drawable/shape_text_login"
            android:gravity="center_vertical"
            android:inputType="textPassword"
            android:maxLines="1"

            android:paddingStart="@dimen/pading_text_login"
            android:paddingEnd="@dimen/pading_text_login"
            android:textColor="@color/colorPrimary"
            android:textSize="15sp"
            android:importantForAutofill="no" />

        <Button
            android:id="@+id/btnIngresar"
            android:layout_width="match_parent"
            android:layout_height="@dimen/height_size_items_login"
            android:layout_marginTop="30dp"
            android:background="@drawable/shape_buttom_login"
            android:text="@string/ingresar"
            android:textColor="@color/white" />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>